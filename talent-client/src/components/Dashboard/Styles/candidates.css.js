import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  candidatesContent: {
    background: 'white',
    height: 'calc(100vh - 0px)',
    float: 'left',
    width: '100%'
  },
  root: {
        width: '100%',
      },
      container: {
        maxHeight: 650,
      }
});