import { makeStyles } from '@material-ui/core';

export const useStyle = makeStyles({
    candidatesContent: {
        background: 'white',
        width: '80%',
        height: 'calc(100vh - 0px)',
        float: 'left'
    }
})